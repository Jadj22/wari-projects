/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
        "./src/**/*.{js,ts,jsx,tsx}", // Inclut tous les fichiers JS, TS, JSX, TSX dans src/
    ],
    theme: {
        extend: {},
    },
    plugins: [],
};